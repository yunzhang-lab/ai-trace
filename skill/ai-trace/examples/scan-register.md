# scan/register example

> status: staging

当前示例仍调用开发入口：

```bash
python3 apps/cli/main.py scan --mock
python3 apps/cli/main.py register --candidate-id candidate-codex --alias Codex
```

未来 Phase 5 若 Skill 正式化，再替换为 `skill/ai-trace/bin/` 下的命令。
